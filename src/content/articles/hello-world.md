---
title: "Hello, world"
description: "Kicking off this site — what I'm planning to write about here."
pubDate: 2026-08-15
tags: ["meta"]
draft: true
---

This is a placeholder article, set to `draft: true` so it won't show up on the
live site until you flip that flag (or delete this file).

To publish your first real article:

1. Copy this file to a new one in `src/content/articles/`, e.g. `my-first-post.md`.
2. Fill in `title`, `description`, `pubDate`, and `tags` in the frontmatter.
3. Write the article body in Markdown below the frontmatter.
4. Set `draft: false` (or remove the line — it defaults to `false`).

Good first topics, given what's on your GitHub: a deep dive on why the
Universal Interface Stack is layered the way it is, a write-up of how the
Tustin/bilinear discretization in `TustinPID` compares to a naive
forward-Euler PID, or a bring-up log for the 5-DOF gripper arm.
